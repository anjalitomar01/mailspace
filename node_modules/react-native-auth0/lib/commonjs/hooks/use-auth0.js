"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;
var _react = require("react");
var _auth0Context = _interopRequireDefault(require("./auth0-context"));
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
/**
 * Use the `useAuth0` in your function components to access authentication state and methods.
 * @returns {Auth0ContextInterface} The useAuth0 hook interface
 *
 * ```ts
 * const {
 *   // State
 *   error,
 *   user,
 *   isLoading,
 *   // Methods
 *   authorize,
 *   sendSMSCode,
 *   authorizeWithSMS,
 *   sendEmailCode,
 *   authorizeWithEmail,
 *   sendMultifactorChallenge,
 *   authorizeWithOOB,
 *   authorizeWithOTP,
 *   authorizeWithRecoveryCode,
 *   hasValidCredentials,
 *   clearSession,
 *   getCredentials,
 *   clearCredentials,
 *   requireLocalAuthentication,
 *   authorizeWithPasswordRealm,
 *   authorizeWithExchangeNativeSocial,
 *   revokeRefreshToken
 * } = useAuth0();
 * ```
 *
 * Refer to {@link Auth0ContextInterface} on how to use the above methods.
 */
const useAuth0 = () => (0, _react.useContext)(_auth0Context.default);
var _default = exports.default = useAuth0;
//# sourceMappingURL=use-auth0.js.map